let lados = 6;

let soma = (lados - 2) * 180;

let valorAngulo = soma / lados;

console.log(`Para um polígono de ${lados} lados, o valor da soma dos angulos internos é de ${soma.toFixed(2)} e o valor do angulo interno é de ${valorAngulo.toFixed(2)}.`);