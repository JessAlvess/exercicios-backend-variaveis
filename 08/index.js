let raio = 1;
let comprimento = 2 * Math.PI * raio;
let area = Math.PI * (raio ** 2);

console.log(`Um círculo de raio ${raio} tem o comprimento de ${comprimento.toFixed(1)} e a área de ${area.toFixed(2)}.`);